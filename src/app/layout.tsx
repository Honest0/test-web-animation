import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Join Our Pack - Email Collection Landing Page',
  description: 'Be the first to know about our exciting updates and exclusive content. Join our community today!',
  keywords: 'email signup, newsletter, community, updates, exclusive content',
  authors: [{ name: 'Your Company' }],
  viewport: 'width=device-width, initial-scale=1',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        {children}
      </body>
    </html>
  )
}



